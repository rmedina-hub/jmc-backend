{
  "name": "jmc-backend",
  "version": "0.1.0",
  "description": "JMC App Premium — Fase 4 backend (Fastify + PostgreSQL). RBAC server-side, optimistic concurrency, sync queue, IA proxy.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node src/migrate.js",
    "test": "node --test",
    "seed": "node src/seed.js",
    "provision": "bash scripts/provision.sh",
    "smoke": "node scripts/prod-smoke.mjs"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.30.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.4"
  }
}
